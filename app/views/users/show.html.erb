<h1>My Events</h1>

<ol>
  <% current_user.events_attending.each do |event| %>
    <li>
      <% @event = Event.find(event) %>
      <%= link_to "#{@event.name}", event_path(@event)%>
    </li>
  <% end %>
</ol>
