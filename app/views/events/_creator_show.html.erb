<h2>Unconfirmed Guests</h2>

<% if event.is_upcoming? %>
  <%= link_to "Edit Event", edit_event_path(event) %>
  <br>
  <%= link_to "Delete Event", event_path(event), data: {
                                                            turbo_method: :delete,
                                                            turbo_confirm: "Are you sure?"
                                                          } %>
<% end %>
