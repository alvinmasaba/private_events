<% if event.is_upcoming? %>
  <% attended_event = EventAttendee.find_by(event_attendee: current_user, attended_event: event) %>

  <%= link_to "Click To Flop", event_attendee_path(attended_event.id), data: {
                                                                        turbo_method: :delete,
                                                                        turbo_confirm: "Are you sure?"
                                                                      }  %>
<% end %>
